import React from 'react';
import ShowList from '../components/ShowList';

const Home = () => {
    return (
        <div>
         <h2>Show List</h2>
         <ShowList/>
        </div>
    );
};

export default Home;